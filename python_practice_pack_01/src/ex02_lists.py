
"""ex02: リスト"""


def even_squares(nums: list[int]) -> list[int]:
    """偶数だけを平方して昇順で返す。入力は変更しない。"""
    raise NotImplementedError


def top_k(nums: list[int], k: int) -> list[int]:
    """大きい順にk個返す（安定でなくてOK、k>len(nums)なら全件）。"""
    raise NotImplementedError


def find_first(predicate, items: list) -> int | None:
    """述語を満たす最初のインデックスを返す。なければNone。"""
    raise NotImplementedError
